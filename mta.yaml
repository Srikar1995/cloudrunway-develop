_schema-version: "3.2"
ID: cloudrunway
description: Generated by Fiori Tools
version: 1.0.4
modules:
- name: cloudrunway-destination-content
  type: com.sap.application.content
  requires:
  - name: cloudrunway-destination-service
    parameters:
      content-target: true
  - name: cloudrunway-repo-host
    parameters:
      service-key:
        name: cloudrunway-repo-host-key
  - name: cloudrunway-uaa
    parameters:
      service-key:
        name: cloudrunway-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: cloudrunway_html_repo_host
          ServiceInstanceName: cloudrunway-html5-service
          ServiceKeyName: cloudrunway-repo-host-key
          sap.cloud.service: cloudrunway
        - Authentication: OAuth2UserTokenExchange
          Name: cloudrunway_uaa
          ServiceInstanceName: cloudrunway-xsuaa-service
          ServiceKeyName: cloudrunway-uaa-key
          sap.cloud.service: cloudrunway
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: cloudrunway-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: cloudrunway-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - cloudrunway.zip
      name: cloudrunway
      target-path: resources/
- name: cloudrunway
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: cloudrunway-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: cloudrunway-destination-service
    service-plan: lite
- name: cloudrunway-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: cloudrunway-xsuaa-service
    service-plan: application
     #config:
          #xsappname: cloudrunway-${org}-${space}
          #tenant-mode: dedicated
- name: cloudrunway-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: cloudrunway-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
